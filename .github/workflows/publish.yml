name: Publish Gem with Trusted Publishing

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to RubyGems
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'

      - name: Build gem
        run: gem build trusted_publishing_test.gemspec

      - name: Publish to RubyGems via Trusted Publishing
        uses: landongrindheim/release-gem@24f073b6d293291cc37ed3f0485efc6f68a03896
